# ===================================================================
# CustomChatAI Environment Configuration
# ===================================================================
#
# IMPORTANT: Most variables are OPTIONAL and will be auto-generated!
# Only set these if you want to override the defaults.
#
# For production, it's recommended to set:
# - NEXTAUTH_SECRET (or let it auto-generate)
# - POSTGRES_PASSWORD (or let it auto-generate)
# - OAuth credentials (if using OAuth)
# ===================================================================

# Database Configuration
# If not set, will use default password "chatpass"
# For production, set a strong password or leave empty to auto-generate
# POSTGRES_PASSWORD=""

# Database URL - will be auto-constructed if not provided
# DATABASE_URL="postgresql://chatuser:${POSTGRES_PASSWORD}@localhost:5432/customchatai"

# LLM API Configuration
# Default: http://model-runner:11434/v1 (auto-configured)
# LLM_API_BASE_URL="http://localhost:11434/v1"

# NextAuth Configuration
# Default: http://localhost:3000 (auto-configured)
# NEXTAUTH_URL="http://localhost:3000"

# NextAuth Secret - WILL BE AUTO-GENERATED if not provided!
# For production, either set this to a strong random value or leave empty to auto-generate
# To manually generate: openssl rand -base64 32
# NEXTAUTH_SECRET=""

# Google OAuth (Optional)
# Only required if you want to enable Google OAuth login
# Get credentials from: https://console.cloud.google.com
# GOOGLE_CLIENT_ID=""
# GOOGLE_CLIENT_SECRET=""

# GitHub OAuth (Optional)
# Only required if you want to enable GitHub OAuth login
# Get credentials from: https://github.com/settings/developers
# GITHUB_CLIENT_ID=""
# GITHUB_CLIENT_SECRET=""

# Rate Limiting (Optional)
# Defaults: 100 requests per 60 seconds (60000ms)
# RATE_LIMIT_REQUESTS=100
# RATE_LIMIT_WINDOW=60000

# ===================================================================
# QUICKSTART: You can deploy without creating a .env file!
# All secrets will be auto-generated at startup.
# Just run: docker compose up -d
# ===================================================================
